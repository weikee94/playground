---
title: Flexbox 布局完全指南
tags: CSS, Layout, 前端
date: 2026-01-31
---

# Flexbox 布局完全指南

Flexbox 是一种强大的一维布局模型,用于在容器中分配空间和对齐项目。

## 🧪 基础概念

Flexbox 由两部分组成:
- **Flex 容器(Container)**: 设置了 `display: flex` 的父元素
- **Flex 项目(Items)**: 容器内的直接子元素

## 核心属性

### 容器属性

**flex-direction**: 定义主轴方向
- `row` (默认): 水平从左到右
- `column`: 垂直从上到下
- `row-reverse`: 水平从右到左
- `column-reverse`: 垂直从下到上

**justify-content**: 主轴对齐方式
- `flex-start`: 起点对齐
- `center`: 居中对齐
- `space-between`: 两端对齐
- `space-around`: 均匀分布

**align-items**: 交叉轴对齐方式
- `stretch` (默认): 拉伸填充
- `center`: 居中对齐
- `flex-start`: 起点对齐
- `flex-end`: 终点对齐

### 项目属性

**flex**: 设置项目的放大缩小比例
```css
.item {
  flex: 1; /* 等分剩余空间 */
}
```

**order**: 改变项目的排列顺序
```css
.item {
  order: 2; /* 数字越小越靠前 */
}
```

## 💡 实用技巧

### 1. 垂直水平居中

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### 2. 圣杯布局

```css
.container {
  display: flex;
}

.sidebar {
  flex: 0 0 200px; /* 固定宽度 */
}

.main {
  flex: 1; /* 占据剩余空间 */
}
```

### 3. 响应式导航栏

```css
.nav {
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap; /* 自动换行 */
}
```

## ✅ 浏览器兼容性

现代浏览器都支持 Flexbox,IE11+ 也有良好的支持(需要前缀)。

## 🚀 下一步

学习完 Flexbox 后,可以探索 CSS Grid,它更适合二维布局。
